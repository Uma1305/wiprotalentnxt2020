import java.io.*;
import java.util.*;
//read only region start
  class UserMainCode
{
            public int seconfLastDigitof(int input1){
              //read only region end
                  input1=((input%100)/10);
                   if(input1<=0){
                       input*=-1;
                   }

                   return (input1);
         }
 }